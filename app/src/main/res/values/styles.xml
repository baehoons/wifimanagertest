<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/exersior_green</item>
        <item name="colorPrimaryDark">@color/jaksim_blue_900</item>
        <item name="colorAccent">@color/exersior_pink</item>
    </style>

    <style name="AlertDialog" parent="Base.ThemeOverlay.AppCompat.Dark">
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/off_white</item>
        <item name="android:colorAccent">#1b5e20</item>
    </style>
</resources>
